def solution(w,h):
    if w == h : return (w * h - w)
    def gcd(a,b):
        if b==0 : return a
        return gcd(b, a%b)

    if w > h :
        l = w
        s = h
    else :
        l = h
        s = w
    
    g = gcd(l,s)
    return w*h-(w+h-g)
    
정사각형이면 격자는 한 변의 길이랑 동일 > 가로 = 세로 면 가로*세로 - 가로

최대 공약수 이용 -> GCD 함수, 유클리드 알고리즘 이용 > b = 0 일 때 까지 재귀
첫 번째 변수가 큰 수 여야 함 > 가로 세로 비교 > l , s 변수로 바꿔서 파라미터로 넣기
>> 직사각형인 경우 대각선이 지나가는 격자는 가로 * 세로 - 최대 공약수

