def solution(string):
    answer = 0
    for sl in range(1, len(string)//2+1):
        ret = ""
        counter = 1
        
        prev = string[:sl]
        
        for i in range(sl, len(string) + sl, sl):
            if prev == string[i : i + sl]:
                counter += 1
            else:
                if counter != 1:
                    ret = ret + str(counter) + prev
                else:
                    ret = ret + prev
                
                prev = string[i : i + sl]
                counter = 1
        answer = min(answer, let(ret))
        
    return min(answer, len(string))


문자열을 분할할 단위 만들기 > for 문으로 만듦 / 최대 절편 길이는 문자열 길이의 절반 이하 (중복 방지)

각 분할 당 비교용 문자열 = ret /  현재까지의 중복 갯수 counter / 이전 문자열 prev
문자열을 잘라가며 비교 > prev랑 현재 잘린 문자열 동일 > counter + 1
다르다 > counter = 1dlaus ret에 문자열 추가 > counter > 1  ret에 문자열 & counter 추가

각 분할 단위 당 가장 짧은 길이를 min으로 
